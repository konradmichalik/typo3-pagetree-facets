# cat=basic; type=boolean; label=Restrict to admins: Only administrators can use the filter modal and tokens.
adminOnly = 0

# cat=basic; type=string; label=Disabled tabs: Comma-separated list of built-in tab identifiers to disable installation-wide (records, ce, activity, doktype, state, translations, seo).
disabledTabs =

# cat=basic; type=string; label=Disabled options: Comma-separated list of individual option identifiers ("tokenKey:value") to disable installation-wide, e.g. "is:editlocked, seo:nofollow". Disables a single value inside a tab without disabling the whole tab.
disabledOptions =

# cat=basic; type=boolean; label=Persist filter for the session: Remember each backend user's current page tree filter for the duration of their session, so it survives a reload or module switch (cleared on logout).
persistFilter = 0

# cat=basic; type=boolean; label=Notice on empty results: Show a hint below the page tree when a filter matches nothing, offering to adjust or reset it.
emptyResultNotice = 1

# cat=advanced; type=boolean; label=Enable raw query tab (power users): Adds a "raw:<table>|<field>=<value>|..." token that matches arbitrary field values on any TCA table the current backend user has table-select access to. Off by default - review the security implications in the README before enabling.
enableRawQueryTab = 0
